apiVersion: v1
kind: Service
metadata:
  name: terraform-drift-agent
  namespace: terraform-drift
  labels:
    app: terraform-drift-agent
spec:
  selector:
    app: terraform-drift-agent
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30080
  type: NodePort
